#include "main.h"
/**
 * mod - calculates the remainder of two given integers
 *
 * @a: first integer
 * @b: second integer
 *
 * Return: remainder of the division
 */
int mod(int a, int b)
{
	int remainder;
	remainder = a % b;
	return remainder;
}
